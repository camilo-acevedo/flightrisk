project:
  name: flightrisk
  seed: 1337

paths:
  data_raw: data/raw
  data_interim: data/interim
  data_features: data/features
  reports: reports

mlflow:
  experiment: flightrisk
  registry_stage: Production

datasets:
  kkbox: configs/data/kkbox.yaml
  orange_belgium: configs/data/orange_belgium.yaml

features:
  default: configs/features/default.yaml

models:
  risk_lgbm: configs/model/risk_lgbm.yaml
  survival_cox: configs/model/survival_cox.yaml
  uplift_tlearner: configs/model/uplift_tlearner.yaml

eval:
  default: configs/eval/default.yaml
